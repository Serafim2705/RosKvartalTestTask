@page
@model RosKvartalTestTask.Pages.Register.IndexModel

@{
    ViewData["Title"] = "Index";
}
@using X.PagedList.Mvc.Core

<h1>Регистр проверок</h1>

@* <p>
    <a asp-page="Create">Create New</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                @* @Html.DisplayNameFor(model => model.InspectionsRegister[0].SubjectName) *@
                Субъект проверки
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.InspectionsRegister[0].SubjectNumber) *@
                ОГРН
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.InspectionsRegister[0].Purpose) *@
                Цель проверки
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.InspectionsRegister[0].Status) *@
                Статус
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.InspectionsRegister[0].Result) *@
                Результат
            </th>
            <th>
                @* @Html.DisplayNameFor(model => model.InspectionsRegister[0].CheckDate) *@
                Дата
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.InspectionsRegister) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SubjectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubjectNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Purpose)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Result)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CheckDate)
            </td>
            <td>
                @* <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a>  *@
                <a asp-page="./Details" asp-route-id="@item.Id">Подробнее</a> 
                @* <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a> *@
            </td>
        </tr>
}
    </tbody>
</table>

@Html.PagedListPager(Model.InspectionsRegister, page => Url.Page("Index", new { page1 = page, pageSize = Model.InspectionsRegister.PageSize }))
@* @Html.PagedListPager(Model.InspectionsRegister, page => Url.Action("Index", new { page })) *@

